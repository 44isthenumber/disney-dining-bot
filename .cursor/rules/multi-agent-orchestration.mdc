---
description: Multi-agent orchestration rules for Magic Table Finder
alwaysApply: true
---

# Multi-Agent Orchestration

This repo uses Cursor as owner and Grok as builder. Goose and Claude are not in any workstream.

- Owner / release: Cursor (desktop or Cloud).
- Builder: Grok 4.6 in Cursor.
- High-risk review: Codex, only for auth, Twilio, VPS, Gist, or destructive git.
- Phone review: Buzz seats when Cursor assigns them. Not required for routine work.

Default workflow:

1. Read `AGENTS.md` before changing production behavior. `CLAUDE.md` is product archive only.
2. Cursor implements. Do not launch Goose or Claude.
3. Cursor owns the final diff, commits, deploys, VPS actions, and production safety.

Safety rules:

- Disney session and availability outrank Cloud convenience. Follow `disney-session-protection.mdc`.
- Never let worker agents mutate `.env`, Gist state, Twilio, Netlify, VPS, git history, or production deployments unsupervised.
- Do not clear `open_slots.json` or `seen_slots.json` unless the user explicitly asks for alert-state repair.
- For alert, worker, or notification changes, run `tests/test_alert_semantics.py`.
- For frontend changes, run an extracted `public/index.html` script through `node --check`.
- Smoke-test with `scripts/smoke_test_api.py` before calling production work done.
